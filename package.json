{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "npm run build && git add . && git commit -m \"update dist\" && git subtree push --prefix dist origin gh-pages && git push"
  },
  "dependencies": {
    "apexcharts": "^3.6.12",
    "core-js": "^2.6.5",
    "vue": "^2.6.10",
    "vue-apexcharts": "^1.3.5"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "^3.7.0",
    "@vue/cli-service": "^3.7.0",
    "node-sass": "^9.0.0",
    "sass-loader": "^7.1.0",
    "vue-template-compiler": "^2.5.21"
  }
}
